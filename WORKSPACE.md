# 工作空间使用规范

本规范定义 `mcp-ssh` 项目中 `tmp/` 与 `workspace/` 两个目录的用途、命名约定与清理策略，所有自动化脚本、子 agent 与人工操作均需遵守。

---

## 📁 目录总览

| 目录 | 用途 | 生命周期 | 是否纳入版本控制 |
|------|------|----------|------------------|
| `tmp/` | 临时文件、中间产物、调试脚本 | 短期，按需清理 | ❌ 不提交 |
| `workspace/` | 长期保留的工作产物、配置与资产 | 长期 | ✅ 提交（敏感信息除外） |

---

## 🗑️ tmp/ — 临时文件夹

### 适用范围
- 一次性调试脚本（如临时状态检查、连通性测试）
- 中间产物（下载后待转存的文件、解压临时目录）
- 验证用的临时数据、日志快照
- 子 agent 执行任务过程中的过渡文件

### 命名约定
- 文件名建议加日期或会话标识前缀，便于识别归属：
  - `check_status_20260705.py`
  - `tmp_upload_<任务名>.bin`
- 避免使用无意义的名字（如 `a.py`、`test.txt`）

### 清理策略
- **会话结束清理**：每次对话或任务结束后，主动清理本会话产生的临时文件
- **定期清理**：建议每周清理一次 `tmp/`，超过 7 天未修改的文件可删除
- **敏感数据立即清理**：含密码、密钥、IP 等敏感信息的临时文件，使用后立即删除，不得残留
- **禁止长期堆积**：`tmp/` 不是归档区，长期需要的文件应迁移到 `workspace/`

### 红线
- ❌ 禁止在 `tmp/` 存放需要长期保留的配置或资产
- ❌ 禁止在 `tmp/` 存放明文密码、私钥、证书等敏感信息
- ❌ 禁止将 `tmp/` 内容提交到 Git 仓库（已在 `.gitignore` 排除）

---

## 📦 workspace/ — 工作空间

### 适用范围
- 长期复用的配置文件（如 `mediamtx.yml`、systemd 服务文件）
- 部署到远端的资产（播放器页面、二进制包、脚本）
- 项目衍生的工作产物（部署脚本、巡检模板、文档）

### 子目录约定

```
workspace/
├── README.md              # 工作空间说明
└── mediamtx/              # MediaMTX 流媒体服务相关文件
    ├── mediamtx.yml       # MediaMTX 配置
    ├── mediamtx.service   # systemd 服务单元
    ├── mediamtx.zip       # 二进制压缩包（可选，体积大可不提交）
    └── player.html        # 网页播放器
```

新增工作主题时，按主题新建子目录，命名使用小写加下划线（如 `rtsp_sources/`、`deploy_scripts/`）。

### 命名约定
- 子目录名：小写英文 + 下划线，语义清晰
- 文件名：保留原始名称，便于与远端部署对应
- 配置文件如有多环境差异，加后缀区分：`mediamtx.prod.yml` / `mediamtx.dev.yml`

### 提交策略
- ✅ 默认提交配置文件、脚本、文档、HTML 等文本资产
- ⚠️ 大体积二进制（如 `mediamtx.zip`）建议加入 `.gitignore`，仅在本地保留
- ❌ 含敏感信息（密码、私钥、Token）的文件禁止提交，使用 `.env.example` 提供模板

### 红线
- ❌ 禁止在 `workspace/` 存放临时调试文件（应放 `tmp/`）
- ❌ 禁止提交明文敏感信息
- ❌ 禁止直接修改部署到生产环境的配置而不留备份（备份命名：`<文件名>.bak.<时间戳>`）

---

## 🔄 文件迁移规则

| 场景 | 操作 |
|------|------|
| 临时文件需要长期保留 | 从 `tmp/` 迁移到 `workspace/` 对应子目录 |
| `workspace/` 文件仅用于一次性测试 | 迁移到 `tmp/`，用后清理 |
| 不确定归属 | 先放 `tmp/`，确认长期需要后再迁移 |
| 子 agent 产生的中间文件 | 默认放 `tmp/`，任务完成后清理 |

---

## 🤖 子 agent 操作约定

调用 mcp-ssh 子 agent 时遵循以下规则，配合 `ark.agent.md` 的原子化操作规范：

1. **临时文件默认放 `tmp/`**：子 agent 生成的调试脚本、中间产物一律写入 `tmp/`，不得污染项目根目录或 `workspace/`
2. **任务结束清理**：子 agent 完成任务后，主动删除本次产生的 `tmp/` 临时文件，并在结果中说明清理情况
3. **资产入库需确认**：子 agent 不得擅自将文件写入 `workspace/`，需经用户确认后再迁移
4. **路径使用绝对路径**：调用 `ssh_upload` / `ssh_download` 时，本地路径使用绝对路径，避免歧义
5. **敏感信息零残留**：含敏感信息的临时文件用后即删，不得在 `tmp/` 残留

---

## ✅ 检查清单

操作完成前自查：

- [ ] 临时文件是否已放入 `tmp/` 而非项目根目录？
- [ ] `tmp/` 中的会话临时文件是否已清理？
- [ ] 长期资产是否已归档到 `workspace/` 对应子目录？
- [ ] 是否有敏感信息残留未清理？
- [ ] 大体积二进制是否已加入 `.gitignore`？
- [ ] 新增子目录是否已更新本规范的目录结构示例？
